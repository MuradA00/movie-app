import { bannerDefaultBg } from "../../variables/images";

const Banner = () => {
    return (
        <section className="h-screen bg-cover bg-no-repeat bg-center" style={{backgroundImage: `url(${bannerDefaultBg})`}}> 
            qwe
        </section>
    )
}

export default Banner;